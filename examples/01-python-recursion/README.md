# Example 1 · Programming: Python Recursion

**Language**: English ｜ [中文](README_CN.md)

- **Output file**: `leap-01-python-recursion.html` (place in this directory)
- **Topic**: Factorial recursion — explanation and practice
- **node_id**: `py.recursion.factorial` · **unit_tag**: `python-recursion`
- **Required interaction components**:
  1. Recursion execution animation (call-stack visualisation, step / reset)
  2. Embedded code runner (editable + run + console output, controlled JS simulation)
  3. Three-level progressive hints
  4. Drag-and-connect item (recursion step ↔ execution phase)
  5. Code completion with instant feedback
- **Reference approaches**: LiaScript CodeRunner, LiaScript DragAndDrop Template
- **Acceptance**: Code is editable and runnable; the stack animation clearly shows recursion depth; feedback identifies conceptual errors.

Structural spec: `../_shared/leap-web-spec.md`. JSON contract: `../_shared/interaction-output.schema.json`.
