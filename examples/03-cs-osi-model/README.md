# Example 3 · Computer Science: OSI Model

**Language**: English ｜ [中文](README_CN.md)

- **Output file**: `leap-03-cs-osi-model.html` (place in this directory)
- **Topic**: Structure and function of the seven OSI network layers
- **node_id**: `cs.network.osi_model` · **unit_tag**: `osi-model`
- **Required interaction components**:
  1. Seven-layer structural diagram (SVG, click a layer to expand its responsibilities)
  2. Drag-and-connect item (protocol / device → matching layer, auto-scored)
  3. Encapsulation / decapsulation animation (play / pause / reset)
  4. Multiple choice with concept-discrimination feedback (≥3 items)
- **Reference approaches**: Canvas flow diagrams, LiaScript DragAndDrop Template
- **Acceptance**: Layers are clear; drag interaction is smooth; the encapsulation animation is intuitive.

Structural spec: `../_shared/leap-web-spec.md`. JSON contract: `../_shared/interaction-output.schema.json`.
