# 示例 3 · 计算机基础：OSI 七层模型

**语言 / Language**：**简体中文** ｜ [English](README.md)

- **产出文件名**：`leap-03-cs-osi-model.html`（放入本目录）
- **主题**：OSI 七层网络模型的分层结构与功能
- **node_id**：`cs.network.osi_model` ｜ **unit_tag**：`osi-model`
- **必含交互组件**：
  1. 七层结构可视化示意图（SVG，可点击展开各层职责）
  2. 拖拽连线题（协议 / 设备 → 对应层级，自动判分）
  3. 数据封装 / 解封装过程动画（可播放 / 暂停 / 重置）
  4. 选择题 + 概念辨析反馈（≥3 题）
- **参考 Skill 思路**：纯 HTML Canvas 流程图、LiaScript DragAndDrop Template
- **验收**：层级清晰；拖拽交互流畅；封装过程动画直观。

规范见 `../_shared/leap-web-spec.md`，JSON 契约见 `../_shared/interaction-output.schema.json`。
